---
title: "[바이브 코딩 #10] AI 주식 시그널 아카이브: 구조 개편과 지능형 성능 최적화"
date: "2026-03-01"
tags: "Streamlit, 트러블슈팅, API최적화, Gemini, 리팩토링, 개발일기"
---

"코딩은 기세다."

**토스 증권 시그널 클론 코딩** 프로젝트가 드디어 10일 차라는 중대한 이정표에 도달했습니다. 어제 구현한 '모델 이원화 전략'을 기반으로, 오늘은 시스템의 구조적 안정성을 확보하고 AI 요약의 품질을 극대화하는 '디테일의 완성'형 작업을 진행했습니다. 10일간의 여정을 돌아보며 마주한 예기치 못한 트러블슈팅과 성능 최적화의 기록입니다.

## 1. 구조 정리와 뜻밖의 배포 오류 (Troubleshooting)

프로젝트가 커짐에 따라 코드의 유지보수성을 높이기 위해 디렉터리 구조를 `backend/`와 `data/` 등으로 체계적으로 정리했습니다. 그런데 여기서 예상치 못한 복병을 만났습니다.

### 문제: "The main module file does not exist"
구조를 정리하고 나니 멀쩡히 돌아가던 Streamlit Cloud에서 배포 오류가 발생했습니다. Streamlit Cloud는 기본적으로 저장소 루트에 있는 `app.py`를 실행 파일로 기대하는데, 내부 구조를 바꾸면서 경로를 찾지 못하게 된 것입니다.

### 해결: 유연한 경로 로직 도입
`app.py`를 다시 루트로 이동시키되, 내부에서 `backend` 모듈을 불러올 때 경로 이슈가 발생하지 않도록 로직을 보강했습니다.

```python
# app.py 상단의 유연한 경로 로직
import os, sys
sys.path.append(os.path.dirname(__file__))
from backend import crawler
```

기존의 경직된 상대 경로 대신 파일 위치를 기반으로 한 동적 경로 추가 방식을 사용하여, 로컬과 클라우드 배포 환경 어디에서나 모듈 참조 오류 없이 안정적으로 작동하도록 만들었습니다.

## 2. 시그널 품질의 복원: '개별 요약'으로의 회귀

최근 성능 향상을 위해 도입했던 '배치 요약(Batch Summarization)'이 오히려 독이 되었습니다. AI의 주의력(Attention)이 여러 종목으로 분산되면서 요약의 구체성이 떨어지는 부작용이 발견되었습니다. 

오늘 과감히 **개별 요약 방식**으로 회귀했습니다. 각 종목당 최대 1500자의 뉴스 컨텍스트를 온전히 할당하여 Gemini가 변동의 핵심 원인을 더 날카롭게 파헤칠 수 있도록 했습니다. 결과적으로 '수급 변화' 같은 평이한 설명 대신, 기업의 구체적인 실적이나 수주 소식을 담은 고품질 시그널 리포트가 다시 생성되기 시작했습니다.

## 3. 할당량(Quota) 방어 작전: Tiered Smart Skip

GitHub Actions를 통해 20분 간격으로 크롤링을 수행하다 보니, 새로운 뉴스가 없음에도 중복해서 API를 호출하는 쿼터 낭비 문제가 대두되었습니다. 이를 해결하기 위해 **'계층형 스킵 로직(Tiered Skip Logic)'**을 구현했습니다.

- **Tier 1 (Instant Skip)**: 기사 목록의 첫 번째 헤드라인이 기존 데이터와 일치하면 즉시 이전 데이터를 재사용합니다.
- **Tier 2 (Summarization Skip)**: AI가 선정한 '최적 기사'가 이미 요약했던 기사라면, 가장 비용이 큰 요약 API 호출 단계만 스킵합니다.

이 지능형 스킵 시스템 덕분에 20분마다 돌아가는 크롤링 루틴에서도 실제 API 호출은 새로운 정보가 떴을 때만 발생하게 되어, 무료 티어의 제약을 기술적으로 극복해냈습니다.

## 4. 휴장일 예외 처리와 UX 개선

마침 3월 1일(삼일절) 연휴와 겹치면서 시장 데이터가 없는 날의 처리도 중요해졌습니다. `holidays` 패키지를 도입해 한국과 미국 시장의 휴장일을 관리하고, 데이터가 없는 날에는 자동으로 "가장 최근 영업일"의 시그널로 안내하는 버튼을 추가하여 사용자 경험을 매끄럽게 다듬었습니다.

## 기록을 마치며

단순한 '수집기'를 넘어, 스스로 판단하고 아끼며 성장하는 '지능형 아카이브'로의 진화를 체감한 하루였습니다. 10일간의 파이썬 기반 기초 공사는 이제 끝났습니다. 내일부터는 이 탄탄한 데이터를 바탕으로 더 멋진 사용자 인터페이스와 확장성을 갖춘 새로운 시스템으로의 전환을 시도해보려 합니다. 우리의 기세는 여전히 뜨겁습니다.

---

## 오늘의 개발 요약

목표: 디렉터리 리팩토링 및 AI 요약 품질 최적화, 쿼터 방어 로직 구현
도구: Python, Streamlit, Gemini API, Holidays Library

*태그: Streamlit, 트러블슈팅, API최적화, Gemini, 리팩토링, 개발일기*
